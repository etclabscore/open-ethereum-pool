<div class="jumbotron-brand">
  <div class="container">
    <div class="row">
      <div class="col-md-5">
        <div class="row">
          <h1 style="font-size: 24px">
            <div class="etc-green">
              <strong><span class="logo-2">ETCTürk Havuzu</span></strong>
            </div>
          </h1>
        </div>
        <div class="row" style="padding:15px 5px;">
          <strong>Min. Ödeme Tutarı: {{config.PayoutThreshold}}</strong>, günde bir kez ödeme yapılır.
          <br/>
          <span class="label label-success">PPLNS</span> Hızlı ve düzenli ödemeler.
        </div>
      </div>
      <div class="col-md-3 stats">
        <div><i class="fa fa-users"></i> Bağlı Madenci: <span id="poolHashrate">{{format-number stats.model.minersTotal}}</span></div>
        <div><i class="fa fa-tachometer"></i> Havuz Hashgücü: <span id="poolHashrate">{{format-hashrate stats.model.hashrate}}</span></div>
        <div><i class="fa fa-money"></i> Havuz Komisyon: <span id="poolFee" class="label label-success">{{config.PoolFee}}</span></div>
        {{#if stats.model.stats.lastBlockFound}}
          <div><i class="fa fa-clock-o"></i> Son Blok: <span>{{format-relative (seconds-to-ms stats.model.stats.lastBlockFound)}}</span></div>
        {{/if}}
      </div>
      <div class="col-md-4 stats">
        <div><i class="fa fa-unlock-alt"></i> Ağ Zorluğu: <span>{{with-metric-prefix stats.difficulty}}</span></div>
        <div><i class="fa fa-tachometer"></i> Ağ Hashgücü: <span>{{format-hashrate stats.hashrate}}</span></div>
        <div><i class="fa fa-bars"></i> Blokzincir No: <span>{{format-number stats.height}}</span></div>
        <div><i class="fa fa-clock-o"></i> Şans: <span>{{format-number stats.roundVariance style='percent'}}</span></div>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="stats">
    <h4>İstatistik &amp; Ödeme Geçmişiniz</h4>
    <div class="input-group">
      {{input value=cachedLogin class="form-control" placeholder="Ethereum Classic Adresinizi Yazın"}}
      <span class="input-group-btn">
        <button class="btn btn-primary btn-etc" type="button" {{action 'lookup' cachedLogin}}>
          <span style="display: inline;"><i class="fa fa-search"></i> Gözat</span>
      </button>
      </span>
    </div>
  </div>
  <div>
    {{#if (equals config.Network 'mordor')}}
      <div class="bs-callout bs-callout-danger">
        <h4>
          Yönergeler
        </h4>
        <p>Tüm madencilere <strong>Bol</strong> kazançlar! Aşağıda bağlantı bilgileri mevcuttur.</p>
      </div>
    {{/if}}
    <div class="jumbotron">
      <div class="container">
        <h3 class="text-center" style="padding: 0 0 5px 0; margin: 0 0 40px 0;"> Instructions</h3>
        <div class="row">
          <div class="col-md-12">
            <p class="lead text-center">
              <a href="#/help" class="ul-link">
                <span class="ul ul-danger">stratum</span><br />
                <code>{{config.StratumHost}}:{{config.StratumPort}}</code>
              </a>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
